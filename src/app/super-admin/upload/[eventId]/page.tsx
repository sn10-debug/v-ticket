"use client";
import UploadFormComponent from "@vibrance/components/UploadFormComponent";

export default function ServerUploadPage() {
    return (
        <section className="flex flex-col items-center justify-center min-h-screen">
            <UploadFormComponent />
        </section>
    );
}
